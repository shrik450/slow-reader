// Drizzle schema entry. Define and export tables here as features land;
// `client.ts` re-exports this module via `import * as schema`. Drop the
// placeholder below once a real table is added.
export type SchemaPlaceholder = never;
